# Loose pins: litellm has tight transitive constraints on httpx/pydantic/openai,
# so we let pip resolve compatible versions rather than fighting it.
fastapi>=0.115
uvicorn[standard]>=0.30
litellm>=1.55
claude-agent-sdk>=0.2
keyring>=25
httpx>=0.27
curl_cffi>=0.7
Pillow>=11
psutil>=6.0
beautifulsoup4>=4.12
python-dotenv>=1.0
pypdf>=5.0
python-multipart>=0.0.9
segno>=1.6
